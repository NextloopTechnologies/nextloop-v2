node_modules
out

# Regenerated by `payload generate:importmap` on every dev/build run.
# Linting it is noise: any fix is overwritten the next time the config changes.
src/app/(payload)/admin/importMap.js
